この画像の表構造をマークダウンで表現して。左端の列に、"S","O","A","P","自由","主"などのラベルがあればそれも抽出して。

## OCR参考テキスト
以下はこの画像に対する高精度OCRの認識結果です。
日付・人名・数値・医薬品名などの文字を読み取る際はこのテキストを優先的に参照してください。
※ OCRテキストの先頭や末尾の数行に、認識失敗によるガベージ文字（意味不明な記号・文字の羅列）が含まれる場合があります。このような行はOCR参照として使用せず、画像を直接読んでください。

```
${ocr_text}
```

## 出力形式

各エントリは以下の構造で出力すること。ヘッダー行のテキストは画像に表示されている通りに正確に記載すること。

## [ヘッダー行のテキスト（例: 2026年05月18日 13:43 内科 診療記録 1版）]

| ラベル | 内容 |
|--------|------|
| 自由 | （本文テキスト） |
| S | （本文テキスト） |
| 1 内服 | （薬剤名・容量など） |

- 画像に表示されているテキストをそのまま抽出すること（補完・推測禁止）
- 画面が途中で始まる・途中で終わる場合も、見えている範囲を表現すること
- 画面の冒頭から最初の ## ヘッダーが出現するまでの間に見えているコンテンツ（テーブル行・テキスト等）は、最初の ## ヘッダーのエントリ本文には含めず、ヘッダーより前に ## なしでそのままテキストとして出力すること（前のエントリの末尾に相当する）
- 複数のエントリが画面に表示されている場合はすべて出力すること
- 「画像が切れている」「以下省略」「原文通り」などの自分自身のアノテーション・注釈を本文に追加しないこと
- 「[済]」タグを含む行（オーダー完了行）とその直後に列挙されている検査名・薬剤名は診療記録の本文ではない。これらを内容列に含めないこと
- ラベル列が「#0」「#1」等の番号形式（EHRのオーダー識別子）の行で、内容が「点滴注射」「内服」「■ ... ■■」形式のオーダーセット名・薬剤名・検査名のみから成る場合は、診療記録の本文ではないため出力しないこと
